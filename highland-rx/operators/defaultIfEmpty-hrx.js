import H from 'highland';
import hrxdefaultIfEmpty from './internal/defaultIfEmpty';

const defaultIfEmpty = hrxdefaultIfEmpty(H);
// eslint-disable-next-line import/prefer-default-export
export { defaultIfEmpty };
